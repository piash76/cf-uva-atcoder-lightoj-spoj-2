#include<bits/stdc++.h>
#define ll long long
#define pi acos(-1)
using namespace std;

int main()

{
    double r,m,c,d,n,a1,a2;
    while(1)
    {
        cin>>r>>m>>c;
        if(r==0 && m==0 && c==0)
        {
            return 0;
        }
        a1=pi*r*r;
        a2=(c/m)*4*r*r;
        cout<<a1<<" "<<a2<<endl;

        if(x==0)
        {
            cout<<""
        }
    }
}
